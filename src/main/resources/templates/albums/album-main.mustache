<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>That Song Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Google Fonts - Noto Sans KR 추가 */
        @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap');

        /* 기본 폰트 설정 */
        body {
            font-family: 'Noto Sans KR', sans-serif;
        }

        /* 다크 모드 스타일 */
        .dark-mode {
            background-color: #1f2937; /* 다크 배경색 */
            color: #d1fae5; /* 민트 텍스트 */
        }

        .dark-mode a {
            color: #6ee7b7;
        }

        .dark-mode a:hover {
            color: #34d399;
        }
    </style>
</head>
<body class="dark-mode font-sans">
<div class="flex min-h-screen">
    <!-- 사이드바 -->
    {{>sidebar}}

    <!-- 메인 콘텐츠 -->
    <main class="flex-1 p-6 transition-all duration-300 md:pl-12 md:pr-12">
        <header class="flex justify-between items-center pb-4 border-b border-teal-700">
            <h1 class="text-2xl font-bold">앨범 관리</h1>
            <a href="/admin/albums/create" class="px-4 py-2 bg-teal-700 text-white rounded hover:bg-teal-600">
                신규 등록
            </a>
        </header>
        {{>albums/album-list}}
    </main>
</div>
</body>
</html>
